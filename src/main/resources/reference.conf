tourstore {
  db {
    url="jdbc:postgresql://localhost:5432/tourstore"
    user="postgres"
    password="ley_ley_ley"
    driver="org.postgresql.Driver"
    connections = {
      poolSize = 10
    }
  }
  server {
    host="0.0.0.0"
    port=8080
  }
}